#!/usr/bin/env sh
# Block push unless full lint and format check pass
npm run lint
npm run format:check
# Releases: run `npm run release` (or release:patch/minor/major) locally, then push with --follow-tags.
# CI creates a GitHub Release when a tag v* is pushed (see .github/workflows/release-from-tag.yml).
