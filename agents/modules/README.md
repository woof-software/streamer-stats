# Module-specific agent instructions

Optional per-module guides for AI agents (e.g. `positions.md`, `vendors.md`). See [AGENTS.md](../AGENTS.md) for the main instructions.

When you create a project from this foundation, add one `.md` file per complex module here if the module needs extra context, protocol details, or constraints for agents.
