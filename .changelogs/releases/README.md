# Release Notes

The main changelog is **CHANGELOG.md** in the repo root (generated by standard-version on release).

This folder is for optional per-release notes or internal release summaries if needed.
